import { BuscarproductosPipe } from './buscarproductos.pipe';

describe('BuscarproductosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarproductosPipe();
    expect(pipe).toBeTruthy();
  });
});
